-- use it to  search for data qiuckly

create index check_first_name on employees (first_name);

create index  check_first_name on 
employees (first_name,last_name);

--drop index

drop index check_first_name on employees;


show indexes;
